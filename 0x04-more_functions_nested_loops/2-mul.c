#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: first digit
 * @b: second digit
 * Return: multiplication of the two digits
 */

int mul(int a, int b)
{
	return (a * b);
}
